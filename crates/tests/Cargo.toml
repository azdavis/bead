[package]
name = "tests"
version = "0.1.0"
edition = "2018"

[lib]
doctest = false

[dependencies]
lex = { path = "../lex" }
lower-neue = { path = "../lower-neue" }
parse = { path = "../parse" }
statics = { path = "../statics" }
